<!-- <script>
    import ViewItems from './ViewItems.svelte';
    import EditDelivery from './EditDelivery.svelte';
  
    let items = [
      { id: 1, truckId: -1 },
      { id: 2, truckId: 1 },
      // Add more items as necessary
    ];
  
    const handleConfirm = (updatedItems) => {
      // Handle the confirmed items (e.g., send to server, update state, etc.)
      console.log('Confirmed Items:', updatedItems);
    };
  </script>
  
  <main>
    <h1>Genetic Algorithm Package Delivery</h1>
    <ViewItems {items} />
    <EditDelivery {items} onConfirm={handleConfirm} />
  </main>
   -->